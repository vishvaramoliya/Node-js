<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Data Form</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: radial-gradient(circle, #1d2671, #c33764);
            color: #f3f4f6;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        form {
            background: #2d2d44;
            border-radius: 15px;
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.3);
            padding: 25px;
            width: 95%;
            max-width: 450px;
        }

        h6 {
            margin: 12px 0 6px;
            font-size: 1rem;
            color: #9fa8da;
        }

        input[type="text"],
        input[type="number"],
        input[type="file"] {
            width: 100%;
            padding: 10px 12px;
            margin: 8px 0 18px;
            border: none;
            border-radius: 8px;
            box-sizing: border-box;
            background: #3c3f58;
            color: #e5e5e5;
            font-size: 0.95rem;
        }

        input[type="file"] {
            padding: 8px;
        }

        img {
            display: block;
            margin: 15px auto;
            border-radius: 8px;
            border: 2px solid #9fa8da;
        }

        button {
            background: #4a69bd;
            color: #f3f4f6;
            border: none;
            border-radius: 8px;
            padding: 12px 20px;
            cursor: pointer;
            font-size: 1rem;
            font-weight: bold;
            transition: background 0.3s ease, transform 0.2s ease;
            display: block;
            margin: 0 auto;
        }

        button:hover {
            background: #1e3799;
            transform: scale(1.05);
        }

        button:active {
            transform: scale(0.98);
        }
    </style>
</head>
<body>
    <form action="/updateData" method="post" enctype="multipart/form-data">
        <input hidden type="text" value="<%=data.id%>" name="id">

        <h6>Movie Name:</h6>
        <input type="number" value="<%=data.vote%>" placeholder="Enter Movie Name" name="vote">

        <h6>Movie Character:</h6>
        <input type="text" value="<%=data.name%>" placeholder="Enter Movie Character" name="name">

        <h6>Movie Director:</h6>
        <input type="text" value="<%=data.genre%>" placeholder="Enter Movie Director" name="genre">

        <h6>Movie Image:</h6>
        <input type="file" name="image">
        <img src="<%=data.image%>" alt="Movie Image" width="30%">

        <button type="submit">Update Data</button>
    </form>
</body>
</html>
