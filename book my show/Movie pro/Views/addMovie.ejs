<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Movies Form</title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #ffecd2, #fcb69f);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
        }

        div {
            background-color: #ffffff;
            border: 1px solid #ddd;
            border-radius: 20px;
            padding: 40px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
            width: 90%;
            max-width: 450px;
            text-align: center;
            transition: box-shadow 0.3s ease, transform 0.2s ease;
        }

        div:hover {
            transform: translateY(-10px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.3);
        }

        h1 {
            color: #ff6f61;
            font-size: 30px;
            margin-bottom: 20px;
            font-family: 'Trebuchet MS', sans-serif;
        }

        input[type="text"],
        input[type="number"],
        input[type="file"] {
            width: 100%;
            padding: 14px;
            margin: 10px 0;
            border: 2px solid #ff6f61;
            border-radius: 8px;
            font-size: 16px;
            background-color: #fff5f2;
            box-sizing: border-box;
            transition: border-color 0.3s ease, background-color 0.3s ease;
        }

        input[type="text"]:hover,
        input[type="number"]:hover,
        input[type="file"]:hover {
            background-color: #ffeae2;
            border-color: #ff856a;
        }

        input[type="text"]:focus,
        input[type="number"]:focus,
        input[type="file"]:focus {
            background-color: #ffe2d6;
            border-color: #ff856a;
            outline: none;
        }

        button {
            width: 100%;
            background: linear-gradient(135deg, #ff6f61, #ff3d2e);
            color: white;
            padding: 15px;
            border: none;
            border-radius: 10px;
            font-size: 18px;
            cursor: pointer;
            font-weight: bold;
            transition: background 0.3s ease, transform 0.2s ease, box-shadow 0.3s ease;
            box-shadow: 0 5px 10px rgba(0, 0, 0, 0.15);
        }

        button:hover {
            background: linear-gradient(135deg, #ff3d2e, #e60000);
            transform: scale(1.05);
            box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
        }

        button:active {
            transform: scale(1);
            box-shadow: 0 3px 6px rgba(0, 0, 0, 0.1);
            background: #cc0000;
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        input[type="file"] {
            background: none;
            padding: 5px;
        }
    </style>
</head>
<body>
    <div>
        <form action="/addData" enctype="multipart/form-data" method="post">
            <h1>Movies</h1>
            <input type="number" placeholder="Movie Rating" name="vote" required>
            <input type="text" placeholder="Movie Name" name="name" required>
            <input type="text" placeholder="Movie Genre" name="genre" required>
            <input type="file" name="image" required>
            <button type="submit">Submit</button>
        </form>
    </div>
</body>
</html>
